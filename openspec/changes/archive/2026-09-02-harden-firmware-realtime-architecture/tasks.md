## 1. Черга команд і задача на Core 1

- [x] 1.1 Визначити структуру команди (тип: volume/source/eq_band/encoder/ir + payload) для FreeRTOS-черги — `firmware/include/command_queue.h`
- [x] 1.2 Створити чергу (`xQueueCreate`) і задачу `xTaskCreatePinnedToCore(..., 1)`, що читає чергу і виконує роботу — `firmware/src/command_queue.cpp`
- [x] 1.3 Перевести BLE-колбеки (`ble_server.cpp`) з прямого виклику `DspController`/`Display`/`LedStrip`/`Storage` на `xQueueSend` — реалізовано в `main.cpp` (колбеки, що реєструються в `BleServer::begin`), сам `ble_server.cpp` не змінювався, бо вже викликав лише function pointer'и
- [x] 1.4 Перевести колбеки енкодера й IR (`inputs.cpp`) на чергу: кнопка енкодера (реальний ISR-контекст) — `xQueueSendFromISR`; оберт енкодера й IR-код (викликаються з `Inputs::poll()`, не ISR) — `xQueueSend`
- [x] 1.5 Перенести `applyVolume`/`applySource`/логіку EQ-обробки з `main.cpp` у обробник черги на Core 1 — `processCommand()` в `main.cpp`, зареєстрований як `CommandQueue::Handler`

## 2. Selfboot ADAU1401

- [x] 2.1 Додати паузу перед першою I2C-транзакцією в `DspController::begin()` — консервативна затримка 200мс
- [ ] 2.2 Перевірити на реальному даташиті ADAU1401 (чи є GPIO/статус-сигнал готовності, чи потрібна лише часова затримка) — **не виконано**: немає доступу до офіційного даташиту в цій сесії; 200мс — консервативне припущення, позначене коментарем у коді (`dsp_controller.cpp`) і Open Question у `design.md`. Потребує підтвердження на реальному залізі.

## 3. RMT-бекенди для WS2812B та IR

- [x] 3.1 Додати явний build-flag для FastLED RMT-драйвера в `platformio.ini` — `-D FASTLED_RMT5=1`, з коментарем, що потребує підтвердження при першій реальній збірці (точна сумісність з версією ESP-IDF не перевірена)
- [ ] 3.2 Підтвердити, що `IRremote @ ^4.4.0` використовує RMT на ESP32 за замовчуванням — **не виконано**: PlatformIO недоступний у цій сесії (`pio`/`platformio` не встановлені), тому неможливо реально зібрати проєкт і перевірити лог бібліотеки. Зафіксовано як припущення коментарем у `inputs.cpp`. Потребує підтвердження під час першої реальної збірки.

## 4. Апаратні примітки (не код)

- [x] 4.1 Зафіксувати в `firmware/README.md` вимогу level shifter (напр. 74AHCT125) на лінії WS2812B DIN
- [x] 4.2 Зафіксувати в `firmware/README.md` вимогу розв'язки живлення PAM8403 (1000+ мкФ конденсатор, зіркова розводка) від наведень WS2812B

## 5. Перевірка

- [ ] 5.1 Зібрати прошивку (`pio run`) з новою чергово-задачною архітектурою, перевірити компіляцію — **не виконано**: PlatformIO недоступний у цій сесії, реальна компіляція неможлива. Код написано за сигнатурами FreeRTOS/Arduino-ESP32 API, але не перевірено компілятором.
- [ ] 5.2 На реальному залізі: підключення по BLE і зміна гучності/EQ під час відтворення музики — без чутних заїкань чи розривів (критерій прийняття з proposal.md) — **не виконано**, заліза немає в цій сесії
- [ ] 5.3 На реальному залізі: перевірити стабільність кольорів WS2812B і коректність декодування IR під активним BLE+A2DP навантаженням — **не виконано**, заліза немає в цій сесії
