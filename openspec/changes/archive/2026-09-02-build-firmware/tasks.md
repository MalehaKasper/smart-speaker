## 1. Каркас проєкту

- [x] 1.1 Створити `platformio.ini` (плата esp32dev, framework arduino, залежності)
- [x] 1.2 Створити `include/config.h` з пінаутом і константами
- [x] 1.3 Розбити код на модулі: `audio_input`, `ble_server`, `dsp_controller`, `inputs`, `display`, `led_strip`, `storage`

## 2. Аудіотракт (Bluetooth -> I2S)

- [x] 2.1 Підключити `ESP32-A2DP`, налаштувати I2S-піни, запустити A2DP Sink
- [ ] 2.2 Перевірити стабільність буфера на реальному залізі протягом тривалого відтворення
- [ ] 2.3 Обробити сценарій розриву Bluetooth-з'єднання (перепідключення, без зависання I2S)

## 3. BLE-сервер

- [x] 3.1 Ініціалізувати NimBLE, створити сервіс і control/notify характеристики
- [ ] 3.2 Розпарсити payload `0x01`/`0x02` і викликати відповідні колбеки (готово); `0x03` наразі парситься як довільний блоб — потребує переробки під `[band_index: 1 байт][5 × float32]` (21 байт), з відкиданням пакетів неправильного розміру чи `band_index` поза 0-7
- [x] 3.3 Реалізувати notify зміни гучності при обертанні енкодера
- [ ] 3.4 Перевірити одночасну стабільну роботу NimBLE (BLE) і Bluedroid (A2DP Classic) на реальному пристрої

## 4. DSP (I2C, Safeload)

- [x] 4.1 Реалізувати I2C-запис через Safeload-послідовність (буфер даних → цільова адреса)
- [ ] 4.2 Підключити `setVolume`/`setSource` до Safeload (готово); `loadEqCoefficients(data, len)` як єдиний блоб-запис застаріло — переробити на `loadEqBand(bandIndex, b0, b1, b2, a1, a2)`, один Safeload-виклик на смугу
- [ ] 4.3 Спроєктувати SigmaStudio-схему: 8 каскадних biquad-комірок (General Second-Order), отримати реальні Safeload-адреси для кожної з 8 смуг (наразі в `dsp_controller.cpp` — заглушки)
- [ ] 4.4 Звірити з даташитом ADAU1401 точну кількість Safeload data-слотів і формат фіксованої коми (орієнтовно 5 слотів / 5.23); реалізувати конвертацію float32 → фіксована кома перед Safeload-записом
- [ ] 4.5 Перевірити відсутність чутних артефактів при зміні гучності/EQ на реальному DSP

## 5. Фізичні входи

- [x] 5.1 Налаштувати переривання енкодера (квадратурне декодування) і кнопки
- [x] 5.2 Підключити `IRremote`, читання сирих кодів пульта
- [ ] 5.3 Зняти реальні коди оригінального пульта і реалізувати мапінг код → дія

## 6. Екран і підсвітка

- [x] 6.1 Реалізувати вивід гучності/джерела на TM1650 по I2C
- [x] 6.2 Реалізувати VU-метр гучності на WS2812B через FastLED (бурштиновий колір)

## 7. Пам'ять (NVS)

- [x] 7.1 Реалізувати збереження/завантаження гучності, джерела через `Preferences`
- [ ] 7.1b Переробити зберігання EQ під 8 незалежних смуг (масив 8 × 5 коефіцієнтів чи окремі ключі); при оновленні однієї смуги в NVS змінюється лише вона
- [x] 7.2 Відновлювати стан при старті (`setup()`) і застосовувати його до DSP/дисплея/стрічки

## 8. Інтеграція та перевірка

- [ ] 8.1 Зібрати і прошити на реальний ESP-WROOM-32, перевірити компіляцію (`pio run`)
- [ ] 8.2 Наскрізний тест: додаток → BLE → DSP → чутна зміна звуку
- [ ] 8.3 Наскрізний тест: фізичний енкодер → DSP → BLE-notify → оновлення UI в додатку
