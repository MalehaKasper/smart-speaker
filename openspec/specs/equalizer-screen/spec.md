# equalizer-screen Specification

## Purpose
TBD - created by archiving change build-mobile-app. Update Purpose after archive.
## Requirements
### Requirement: Інтерактивний графік АЧХ
Екран еквалайзера SHALL відображати інтерактивний графік амплітудно-частотної характеристики (АЧХ), що відповідає поточним налаштуванням EQ.

#### Scenario: Графік оновлюється при зміні точки
- **WHEN** користувач змінює параметри однієї з точок еквалайзера
- **THEN** графік АЧХ негайно перемальовується відповідно до нових коефіцієнтів

### Requirement: Ручне керування 8 фіксованими точками параметричного еквалайзера
Екран еквалайзера SHALL надавати рівно 8 точок параметричного еквалайзера (відповідно до 8 смуг DSP) і дозволяти користувачу вручну змінювати Gain, Q-factor і Frequency кожної з них. Додавання точок понад ці 8 SHALL NOT підтримуватися.

#### Scenario: Зміна однієї з 8 точок
- **WHEN** користувач змінює Gain, Q-factor або Frequency однієї з 8 точок
- **THEN** додаток перераховує біквадратні коефіцієнти саме цієї точки і надсилає їх на колонку окремим `0x03`-пакетом

### Requirement: Запуск автоматичної рум-корекції
Екран еквалайзера SHALL містити кнопку запуску автоматичної рум-корекції, яка ініціює аналіз акустики приміщення.

#### Scenario: Кнопка запускає повний цикл авто-корекції
- **WHEN** користувач натискає кнопку "Автоматична рум-корекція"
- **THEN** додаток послідовно виконує калібрувальний сигнал, запис, FFT-аналіз, побудову цільової кривої та розрахунок коефіцієнтів, після чого оновлює графік АЧХ і точки EQ

### Requirement: Список іменованих пресетів еквалайзера
Екран еквалайзера SHALL надавати список іменованих пресетів: зберегти поточні налаштування 8 смуг під новою назвою, обрати й застосувати існуючий пресет, видалити пресет.

#### Scenario: Збереження поточних налаштувань як пресету
- **WHEN** користувач тисне "Зберегти як..." і вводить назву
- **THEN** поточні значення всіх 8 смуг зберігаються під цією назвою і з'являються в списку пресетів

#### Scenario: Застосування пресету
- **WHEN** користувач обирає пресет зі списку
- **THEN** усі 8 смуг оновлюються значеннями з пресету, і кожна змінена смуга надсилається на колонку окремим `0x03`-пакетом

#### Scenario: Видалення пресету
- **WHEN** користувач видаляє пресет зі списку
- **THEN** цей пресет більше не з'являється в списку, поточні застосовані на колонці налаштування не змінюються

